# ── Rust (drs-core) ───────────────────────────────────────────────────────────
target/
**/*.rs.bk
*.pdb
**/mutants.out*/

# ── Go (drs-verify) ───────────────────────────────────────────────────────────
# Compiled binaries
drs-verify/bin/
drs-verify/drs-verify

# ── TypeScript (drs-sdk) ──────────────────────────────────────────────────────
drs-sdk/node_modules/
drs-sdk/dist/
drs-sdk/.tsbuildinfo
drs-sdk/coverage/

# ── Editor / OS ───────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Secrets / credentials (never commit these) ────────────────────────────────
*.key
*.pem
*.env
.env.*
!.env.example

# ── DR Store runtime data (Tier 1 filesystem store) ──────────────────────────
dr-store/

# ── Project files excluded from repo ─────────────────────────────────────────
reddit_post.md
CLAUDE.md
docs-site/book/
