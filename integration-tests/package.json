{
  "name": "drs-integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end integration tests against published DRS artifacts.",
  "scripts": {
    "test": "node --test tests/",
    "test:e2e": "./run.sh"
  },
  "dependencies": {
    "@drs/mcp-server": "workspace:*",
    "@okeyamy/drs-sdk": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
